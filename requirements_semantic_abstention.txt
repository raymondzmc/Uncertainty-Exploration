# Requirements for Semantic Entropy + AbstentionBench integration

# Core dependencies
torch>=2.0.0
transformers>=4.35.0
accelerate>=0.24.0
datasets>=2.14.0

# Scientific computing
numpy>=1.24.0
scikit-learn>=1.3.0

# Progress bars and utilities
tqdm>=4.66.0

# Optional: For faster tokenizers
tokenizers>=0.14.0

# Note: Make sure you have CUDA installed if using GPU
# torch should be installed with CUDA support, e.g.:
# pip install torch --index-url https://download.pytorch.org/whl/cu118 